import { webviewUiToolkitTunnel } from './webview-ui-toolkit-tunnel';

describe('webviewUiToolkitTunnel', () => {
  it('should work', () => {
    expect(webviewUiToolkitTunnel()).toEqual('webview-ui-toolkit-tunnel');
  });
});
